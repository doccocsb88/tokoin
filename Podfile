# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tokoin' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tokoin
  pod 'RxSwift', '~> 5.0'
  pod 'Alamofire', '~> 5.0'
  pod 'SDWebImage', '~> 5.0'

  target 'TokoinTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TokoinUITests' do
    # Pods for testing
  end

end
